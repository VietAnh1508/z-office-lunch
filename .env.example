# Copy to .env. Used by packages/db (drizzle-kit, migrations) and test runs
# against the local Postgres started by `docker compose up -d`.
DATABASE_URL=postgres://postgres:postgres@localhost:5432/office_lunch
