# Architecture

Not decided yet. Tech stack, data model, and directory layout for the lunch app are a separate discussion from the meta-workflow framework — see project conversation history / the next planning pass.

Once decided, this file should hold:
- The data model (entities and how they relate).
- The stack choice per layer (backend, storage, frontend, test tooling) with a one-line justification each.
- Anything a fresh session needs to understand the shape of the app that isn't obvious from reading the code directly.

Do not duplicate per-task implementation detail here — that belongs in the relevant `tasks/*.md` file and the code itself. This file is for decisions that outlive any single task.
